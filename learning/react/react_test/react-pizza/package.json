{
  "name": "yaphi-react-pizza",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "5.0.1",
    "react-transition-group": "^2.2.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "react-scripts build && now alias $(cd build && now --public) yaphi-react-pizza",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
